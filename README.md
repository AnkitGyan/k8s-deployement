# Full Stack Kubernetes Project

This project demonstrates a **full-stack backend system** running on Kubernetes with:

- Node.js (Custom App)
- PostgreSQL
- MongoDB
- Redis
- Kubernetes (KIND cluster)
- Docker &

## Run Project Locally

Follow these steps to run the full stack (Node.js + PostgreSQL + MongoDB + Redis)

---

##  1. Prerequisites

Make sure you have installed:

- Docker
- Docker Compose
- Node.js (optional for local dev)

Check installation:

```bash
docker -v
docker-compose -v